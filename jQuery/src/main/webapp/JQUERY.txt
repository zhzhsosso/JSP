JSON
>자바스크립트로 객체를 표현하는 방법

XML,CSV,JSON (개방형 표준 데이터 포맷)
- 단순 텍스트 형태 이름:값 (직관적)
- 불필요한 태그 없음 => 데이터의 크기가 적음
- 특정 언어에 독립적이다. 규칙을 지켜야함(엄격함)
- JSON 파서는 브라우저에 포함

* {} :JSON 객체
* [] :JSON 배열
* 이름 : 값    : 데이터

* 정수값(2/8/16진수 x)
* 실수값
* boolean
* null
* 문자열 ("")
* 배열, 객체 